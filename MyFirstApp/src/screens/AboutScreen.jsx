import React from 'react';
import MainLayout from "../layouts/MainLayout";
import { Text } from 'react-native';

export default function AboutScreen({  }) {
    return (
    <MainLayout>
        <Text>This is a simple Todo list app created by Adam McDougal.</Text>
    </MainLayout>
    );
}